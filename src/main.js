const Rounter = require("./router/router");
const http = require("http");

class err4 {
    data;
    constructor(options) {
        function error(err4) {
            return undefined;
        }

        this.err4 = err4(this.merror)

        this.err4({
            listen(...args) {
                const err4 = this.listen()
                return;
            }
        })

        // server error
        this.server = http.createServer();
        const err4 = (type) => {
            server_close.call(this.server, this.err4[this.data]);

        };

        const server_close = this.server.close;


        const err4_error = new Rounter(options);
        this.err4 = err4_error.primata;
        this.merror =  err4_error.error;

    }


}


module.exports = options => new primata(options);
